# services:
#   api:
#     build: .
#     container_name: taxi-tip-api
#     ports:
#       - "8000:8000"
#     environment:
#       - MODEL_PATH=/app/models/linear_regression_artifact.pkl
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
services:
  api:
    build: .
    container_name: taxi-tip-api
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/models/linear_regression_artifact.pkl
    depends_on:
      - mlflow
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.12.1
    container_name: mlflow-server
    ports:
      - "5000:5000"
    command: mlflow server --host 0.0.0.0 --port 5000
